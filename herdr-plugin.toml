id = "herdr.remote"
name = "Herdr Remote"
version = "0.1.0"
min_herdr_version = "0.7.0"
description = "Push agent status to a remote relay for mobile/desktop monitoring"
platforms = ["macos", "linux"]

[[events]]
on = "pane.agent_status_changed"
command = ["python3", "on_event.py"]
